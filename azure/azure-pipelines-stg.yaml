trigger:
  branches:
    include:
      - staging

pr: none

pool:
  vmImage: ubuntu-latest

variables:
  - template: templates/variables.yaml
  - name: environment
    value: stg

stages:
  - template: templates/workflow.yaml
